
@{
    Layout = "~/Views/Shared/_ColumnsOneLayout.cshtml";
    ViewData["Title"] = "تائید مشخصات کاربری";
}

<div class="card mt-5">
    <div class="card-header">
        <h5 class="card-title">@ViewData["Title"]</h5>
    </div>
    <div class="card-body">
        با تشکر.
        اطلاعات کاربری شما با موفقیت تائید شدند.
        @if (!User.Identity.IsAuthenticated)
        {
            <span>لطفا جهت ورود به سیستم <a asp-controller="Account" asp-action="Login">اینجا</a> کلیک کنید.</span>
        }
    </div>
</div>

